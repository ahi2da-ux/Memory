# 💻 Developer — 미니 번들 옵션(2안/3안 조합)의 MVP 기능을 최우선으로 구현하고, 사용자 재방문율 향상을 위한 알림 및 리워드 시스템의 최소 기능 구현(MVP)을 개발팀에 요청하여 작업 우선순위를 지정하라.

💻 Developer: 작업 시작합니다.

## 개발팀 작업 우선순위 및 요구사항 정의

CEO의 지시사항("미니 번들 옵션(2안/3안 조합) MVP 구현" 및 "알림/리워드 시스템 MVP 구현")을 기반으로, 개발팀에 전달할 최우선 작업 목록과 기술적 요구사항을 정의합니다. 이는 기존의 시스템 안정성 확보(P0)를 전제로 하며, 신규 기능 구현에 필요한 최소한의 범위(MVP)를 설정하는 데 중점을 둡니다.

### 1. 핵심 우선순위 (P0: 시스템 안정성 기반)
모든 신규 기능 개발에 앞서, 기존 트랜잭션 원자성 확보 로직 및 E2E 검증이 완벽히 보장되어야 합니다.

*   **P0-1: 기존 Lock 메커니즘 최종 검증:** `sessions/2026-05-08T17-47/developer.md`에 정의된 트랜잭션 안정성(Lock)이 새로운 기능(번들, 리워드) 흐름에서 오류 없이 작동하는지 확인하는 통합 테스트 케이스를 최우선으로 실행합니다. (Acceptance Test Case TC-05 중심)
*   **P0-2: 데이터 추적 환경 검증:** `sessions/2026-05-09T00-05/developer.md`에 구축된 KPI 로깅 시스템이 신규 번들 및 리워드 이벤트 발생 시 모든 변수를 누락 없이 정확히 기록하는지 E2E 최종 검증을 수행합니다.

### 2. MVP 기능 정의 (P1: 핵심 가치 구현)

#### A. 미니 번들 옵션 MVP 요구사항
**목표:** 사용자에게 '가치 있는 조합' 경험 제공 및 데이터 흐름 안정성 확보.

| ID | 기능 영역 | 최소 기능 정의 (MVP Scope) | 기술적 고려 사항 (Developer Focus) |
| :--- | :--- | :--- | :--- |
| **B1** | 번들 구성 | 사용자가 2안 또는 3안 중 선택한 조합을 앱 내에서 명확하게 표시하고, 해당 조합에 따른 최종 리워드/할인 금액이 즉시 계산되어야 함. | 가격 정책(Pricing Logic)의 원자적 연산 보장. 데이터베이스 트랜잭션 범위 확정. |
| **B2** | 번들 적용 | 사용자가 선택한 번들을 구매하거나 활성화했을 때, 해당 포인트/재화가 즉시 사용자 계정에 반영되어야 함. | `SELECT FOR UPDATE` 또는 동시성 제어를 통해 재고(포인트) 감소 및 사용자 잔액 증가를 단일 트랜잭션으로 묶어 처리. (P0 기반 적용) |
| **B3** | 번들 기록 | 사용자가 선택한 번들의 히스토리(구매/활성화 시점, 포함된 항목)가 `kpi_log` 또는 별도 테이블에 정확히 기록되어야 함. | 데이터 정합성 검증 (P0-2 결과 활용). |

#### B. 알림 및 리워드 시스템 MVP 요구사항
**목표:** 사용자 재방문율 향상을 위한 능동적인 참여 유도.

| ID | 기능 영역 | 최소 기능 정의 (MVP Scope) | 기술적 고려 사항 (Developer Focus) |
| :--- | :--- | :--- | :--- |
| **R1** | 알림 발송 | 특정 조건(예: 포인트 적립 완료, 챌린지 달성, 신규 번들 출시) 발생 시 사용자에게 푸시 알림을 전송하는 기능. | Supabase Realtime 또는 외부 FCM/APNS 연동의 최소 구현. |
| **R2** | 리워드 지급 | 사용자가 목표를 달성하거나 특정 행동(번들 구매 등)을 완료했을 때, 정의된 규칙에 따라 포인트나 보상을 지급하는 로직. | B2와 동일하게 트랜잭션 원자성 필수 확보. 지급 내역은 `kpi_log`에 명확히 기록되어야 함. |
| **R3** | MY 탭 연동 | 알림 및 리워드 상태가 사용자의 'MY 탭'에서 실시간으로 반영되어야 함. | 데이터베이스 조회 로직 최적화 및 프론트엔드와의 API 응답 구조 확정. |

### 3. 개발팀 작업 분배 지시

**최우선 작업:** **B2 (번들 적용)** 와 **R2 (리워드 지급)** 로직에 대해 P0 기반의 트랜잭션 안정성(Lock 메커니즘)을 적용하여 구현하고, 이 과정에서 발생하는 모든 데이터 변경이 `kpi_log`에 정확히 기록되는지 검증하는 통합 테스트 케이스를 작성 및 실행하십시오.

**후속 작업:** B1 (번들 구성)의 UI/UX 흐름과 R1 (알림)의 최소한의 발송 로직을 다음 단계로 준비하여, P0 안정성 확보 후 순차적으로 진행하십시오.

📝 다음 단계: 개발팀은 위 정의된 MVP 요구사항(B1, B2, B3, R1, R2, R3)에 대해 기술 스펙 및 구현 계획을 작성하고, P0-1/P0-2 결과에 기반한 통합 테스트 시나리오를 제출하십시오.
